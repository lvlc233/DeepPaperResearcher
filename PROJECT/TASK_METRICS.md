# 任务指标
## v0.1
### 后端

第一个功能就是可靠的数据源的查询和外部加载，现代论文主要以PDF文件为核心，并且具有一定的领域性质，也就是说，论文的可发布区域有限制，因此我们首先要支持不同来源的论文获取。
1.  调研如何实现 `SPECIFICATION.md`## 功能需求中的第一个功能，支持从不同的来源，指定网页/本地PDF上传的方式支持不同来源的论文获取功能，并将调查结果存储起来。[ ]
    创建时间: 2026-01-01 6:30
    完成时间: ...
    负责人: BackendAgent
    验收标准:
    - `PROJECT/SPECIFICATION.md` 中补全“论文来源获取（v0.1）”的调研结论与约束（来源类型、能力边界、错误处理、安全约束、大小/并发限制、数据流）。
    - 输出明确的 API 草案（请求/响应字段与错误码）与数据模型草案（表/字段/索引/唯一约束）。
    - 明确 v0.1 不做的范围（需要登录/反爬/付费墙/复杂 JS 渲染页面等）与后续扩展点（arXiv/DOI/Semantic Scholar 等）。

2.  基于调研结果构建数据库表结构和对应的service和controller。完成第一个模块的搭建。[ ]
    创建时间: 2026-01-01 6:30
    完成时间: ...
    负责人: BackendAgent
    验收标准:
    - 数据层: 完成 SQLModel 实体（论文、来源/抓取任务、文件存储引用），包含必要索引与唯一约束，具备可迁移的建表方式。
    - 基础设施层: 完成 PG 访问封装（会话管理、基础 CRUD、事务边界），具备可复用的查询/写入接口。
    - service 层: 完成“创建论文来源任务/上传/抓取/入库/状态更新”的核心编排（无模拟数据）。
    - controller 层: 提供 2 个对外入口（本地 PDF 上传、URL/网页导入），并返回统一响应结构与可追踪的任务/论文 ID。
    - 测试: 至少覆盖 1 条成功路径与 1 条失败路径（例如 URL 非 PDF 且无法提取链接），并保持 TODO 状态直到测试通过。
